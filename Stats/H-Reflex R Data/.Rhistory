effect_size <- cohen.ES(test = "f", size = "medium")
power_analysis <- pwr.anova.test(k = length(groups), f = effect_size$effect.size, sig.level = 0.05, power = 0.8)
print(power_analysis)
# Fit linear mixed-effects model
lme_model <- lmer(Post_H_Reflex ~ Group + (1 | ID), data = data)
print(summary(lme_model))
# Obtain ANOVA table for the mixed model
anova_lme <- anova(lme_model)
print(anova_lme)
# Write an explanation of results
cat("\nExplanation of Results:\n")
cat("The ANOVA results indicate whether there is a statistically significant difference in H-reflex amplitude across the four groups.\n")
cat("If the p-value from the ANOVA is less than 0.05, we reject the null hypothesis, which states there is no difference between groups.\n")
cat("The post-hoc Tukey test will specify which groups have significant differences in H-reflex amplitude.\n")
# Fit linear mixed-effects model
lme_model <- lmer(Post_H_Reflex ~ Group + (1 | ID), data = data)
print(summary(lme_model))
# Load necessary libraries
library(ggplot2)
library(dplyr)
library(pwr)
library(lme4)
library(car)
# Set seed for reproducibility
set.seed(42)
# Simulate sample data
n_per_group <- 15  # Number of participants per group
groups <- c("HROC_only", "VNS_only", "HROC_VNS", "SCI_only")
data <- data.frame(
ID = 1:(n_per_group * length(groups)),
Group = rep(groups, each = n_per_group),
Pre_H_Reflex = rnorm(n_per_group * length(groups), mean = 0.8, sd = 0.1),
Post_H_Reflex = c(
rnorm(n_per_group, mean = 0.85, sd = 0.12),
rnorm(n_per_group, mean = 0.88, sd = 0.12),
rnorm(n_per_group, mean = 1.0, sd = 0.12),
rnorm(n_per_group, mean = 0.78, sd = 0.12)
)
)
# View summary statistics
print(summary(data))
# Visualize pre- and post-intervention data by group
plot <- ggplot(data, aes(x = Group, y = Post_H_Reflex, fill = Group)) +
geom_boxplot() +
theme_minimal() +
labs(title = "Post H-Reflex Amplitudes Across Treatment Groups",
y = "H-Reflex Amplitude (mV)")
print(plot)
# Load necessary libraries
library(ggplot2)
library(dplyr)
library(pwr)
library(lme4)
library(car)
# Set seed for reproducibility
set.seed(42)
# Simulate sample data
n_per_group <- 15  # Number of participants per group
groups <- c("HROC_only", "VNS_only", "HROC_VNS", "SCI_only")
data <- data.frame(
ID = 1:(n_per_group * length(groups)),
Group = rep(groups, each = n_per_group),
Pre_H_Reflex = rnorm(n_per_group * length(groups), mean = 0.8, sd = 0.1),
Post_H_Reflex = c(
rnorm(n_per_group, mean = 0.85, sd = 0.12),
rnorm(n_per_group, mean = 0.88, sd = 0.12),
rnorm(n_per_group, mean = 1.0, sd = 0.12),
rnorm(n_per_group, mean = 0.78, sd = 0.12)
)
)
# View summary statistics
print(summary(data))
# Visualize pre- and post-intervention data by group
plot <- ggplot(data, aes(x = Group, y = Post_H_Reflex, fill = Group)) +
geom_boxplot() +
theme_minimal() +
labs(title = "Post H-Reflex Amplitudes Across Treatment Groups",
y = "H-Reflex Amplitude (mV)")
print(plot)
# Load necessary libraries
library(ggplot2)
library(dplyr)
library(pwr)
library(lme4)
library(car)
# Set seed for reproducibility
set.seed(42)
# Simulate sample data
n_per_group <- 15  # Number of participants per group
groups <- c("SCI_only", "HROC_only", "VNS_only", "HROC_VNS")
data <- data.frame(
ID = 1:(n_per_group * length(groups)),
Group = rep(groups, each = n_per_group),
Pre_H_Reflex = rnorm(n_per_group * length(groups), mean = 0.8, sd = 0.1),
Post_H_Reflex = c(
rnorm(n_per_group, mean = 0.85, sd = 0.12),
rnorm(n_per_group, mean = 0.88, sd = 0.12),
rnorm(n_per_group, mean = 1.0, sd = 0.12),
rnorm(n_per_group, mean = 0.78, sd = 0.12)
)
)
# View summary statistics
print(summary(data))
# Visualize pre- and post-intervention data by group
plot <- ggplot(data, aes(x = Group, y = Post_H_Reflex, fill = Group)) +
geom_boxplot() +
theme_minimal() +
labs(title = "Post H-Reflex Amplitudes Across Treatment Groups",
y = "H-Reflex Amplitude (mV)")
print(plot)
# Load necessary libraries
library(ggplot2)
library(dplyr)
library(pwr)
library(lme4)
library(car)
# Set seed for reproducibility
set.seed(42)
# Simulate sample data
n_per_group <- 15  # Number of participants per group
groups <- c("SCI_only", "HROC_only", "VNS_only", "HROC_VNS")
data <- data.frame(
ID = 1:(n_per_group * length(groups)),
Group = rep(groups, each = n_per_group),
Pre_H_Reflex = rnorm(n_per_group * length(groups), mean = 0.8, sd = 0.1),
Post_H_Reflex = c(
rnorm(n_per_group, mean = 0.78, sd = 0.12)
rnorm(n_per_group, mean = 0.85, sd = 0.12),
# View summary statistics
print(summary(data))
# Visualize pre- and post-intervention data by group
plot <- ggplot(data, aes(x = Group, y = Post_H_Reflex, fill = Group)) +
geom_boxplot() +
theme_minimal() +
labs(title = "Post H-Reflex Amplitudes Across Treatment Groups",
y = "H-Reflex Amplitude (mV)")
print(plot)
# Load necessary libraries
library(ggplot2)
library(dplyr)
library(pwr)
library(lme4)
library(car)
# Set seed for reproducibility
set.seed(42)
# Simulate sample data
n_per_group <- 15  # Number of participants per group
groups <- c("HROC_only", "VNS_only", "HROC_VNS", "SCI_only")
data <- data.frame(
ID = 1:(n_per_group * length(groups)),
Group = rep(groups, each = n_per_group),
Pre_H_Reflex = rnorm(n_per_group * length(groups), mean = 0.8, sd = 0.1),
Post_H_Reflex = c(
rnorm(n_per_group, mean = 0.85, sd = 0.12),
rnorm(n_per_group, mean = 0.88, sd = 0.12),
rnorm(n_per_group, mean = 1.0, sd = 0.12),
rnorm(n_per_group, mean = 0.78, sd = 0.12)
)
)
# View summary statistics
print(summary(data))
# Visualize pre- and post-intervention data by group
plot <- ggplot(data, aes(x = Group, y = Post_H_Reflex, fill = Group)) +
geom_boxplot() +
theme_minimal() +
labs(title = "Post H-Reflex Amplitudes Across Treatment Groups",
y = "H-Reflex Amplitude (mV)")
print(plot)
# Perform paired t-tests within each group
results_t_tests <- data %>%
group_by(Group) %>%
summarise(
t_test_result = list(t.test(Post_H_Reflex, Pre_H_Reflex, paired = TRUE)),
p_value = t_test_result[[1]]$p.value
)
print(results_t_tests)
# Run one-way ANOVA to compare means between groups
anova_result <- aov(Post_H_Reflex ~ Group, data = data)
print(summary(anova_result))
# Normality check using Shapiro-Wilk test
normality_check <- by(data$Post_H_Reflex, data$Group, function(x) shapiro.test(x)$p.value)
print(normality_check)
# Homogeneity of variances using Levene's Test
levene_test_result <- leveneTest(Post_H_Reflex ~ Group, data = data)
print(levene_test_result)
# Tukey's HSD test for post-hoc comparisons
tukey_result <- TukeyHSD(anova_result)
print(tukey_result)
# Conduct power analysis
effect_size <- cohen.ES(test = "f", size = "medium")
power_analysis <- pwr.anova.test(k = length(groups), f = effect_size$effect.size, sig.level = 0.05, power = 0.8)
print(power_analysis)
# Obtain ANOVA table for the mixed model
anova_lme <- anova(lme_model)
print(anova_lme)
# Write an explanation of results
cat("\nExplanation of Results:\n")
cat("The ANOVA results indicate whether there is a statistically significant difference in H-reflex amplitude across the four groups.\n")
cat("If the p-value from the ANOVA is less than 0.05, we reject the null hypothesis, which states there is no difference between groups.\n")
cat("The post-hoc Tukey test will specify which groups have significant differences in H-reflex amplitude.\n")
# Set seed for reproducibility
set.seed(42)
# Simulate sample data
n_per_group <- 15  # Number of participants per group
groups <- c("HROC_only", "VNS_only", "HROC_VNS", "SCI_only")
data <- data.frame(
ID = 1:(n_per_group * length(groups)),
Group = rep(groups, each = n_per_group),
Pre_H_Reflex = rnorm(n_per_group * length(groups), mean = 0.8, sd = 0.1),
Post_H_Reflex = c(
rnorm(n_per_group, mean = 0.85, sd = 0.12),
rnorm(n_per_group, mean = 0.88, sd = 0.12),
rnorm(n_per_group, mean = 1.0, sd = 0.12),
rnorm(n_per_group, mean = 0.78, sd = 0.12)
)
)
# View summary statistics
print(summary(data))
# Perform paired t-tests within each group
results_t_tests <- data %>%
group_by(Group) %>%
summarise(
t_test_result = list(t.test(Post_H_Reflex, Pre_H_Reflex, paired = TRUE)),
p_value = t_test_result[[1]]$p.value
)
print(results_t_tests)
# View summary statistics
print(summary(data))
# Visualize pre- and post-intervention data by group
plot <- ggplot(data, aes(x = Group, y = Post_H_Reflex, fill = Group)) +
geom_boxplot() +
theme_minimal() +
labs(title = "Post H-Reflex Amplitudes Across Treatment Groups",
y = "H-Reflex Amplitude (mV)")
print(plot)
# Run one-way ANOVA to compare means between groups
anova_result <- aov(Post_H_Reflex ~ Group, data = data)
print(summary(anova_result))
# Normality check using Shapiro-Wilk test
normality_check <- by(data$Post_H_Reflex, data$Group, function(x) shapiro.test(x)$p.value)
print(normality_check)
# Homogeneity of variances using Levene's Test
levene_test_result <- leveneTest(Post_H_Reflex ~ Group, data = data)
print(levene_test_result)
# Tukey's HSD test for post-hoc comparisons
tukey_result <- TukeyHSD(anova_result)
print(tukey_result)
# Conduct power analysis
effect_size <- cohen.ES(test = "f", size = "medium")
power_analysis <- pwr.anova.test(k = length(groups), f = effect_size$effect.size, sig.level = 0.05, power = 0.8)
print(power_analysis)
# Obtain ANOVA table for the mixed model
anova_lme <- anova(lme_model)
print(anova_lme)
# Conduct power analysis
effect_size <- cohen.ES(test = "f", size = "medium")
power_analysis <- pwr.anova.test(k = length(groups), f = effect_size$effect.size, sig.level = 0.05, power = 0.8)
print(power_analysis)
# Write an explanation of results
cat("\nExplanation of Results:\n")
cat("The ANOVA results indicate whether there is a statistically significant difference in H-reflex amplitude across the four groups.\n")
cat("If the p-value from the ANOVA is less than 0.05, we reject the null hypothesis, which states there is no difference between groups.\n")
cat("The post-hoc Tukey test will specify which groups have significant differences in H-reflex amplitude.\n")
# Load necessary libraries
library(ggplot2)
library(dplyr)
library(pwr)
library(lme4)
library(car)
# Set seed for reproducibility
set.seed(42)
# Simulate sample data
n_per_group <- 15  # Number of participants per group
groups <- c("HROC_only", "VNS_only", "HROC_VNS", "SCI_only")
data <- data.frame(
ID = 1:(n_per_group * length(groups)),
Group = rep(groups, each = n_per_group),
Pre_H_Reflex = rnorm(n_per_group * length(groups), mean = 0.8, sd = 0.1),
Post_H_Reflex = c(
rnorm(n_per_group, mean = 0.85, sd = 0.12),
rnorm(n_per_group, mean = 0.88, sd = 0.12),
rnorm(n_per_group, mean = 1.0, sd = 0.12),
rnorm(n_per_group, mean = 0.78, sd = 0.12)
)
)
# View summary statistics
print(summary(data))
# Visualize pre- and post-intervention data by group
plot <- ggplot(data, aes(x = Group, y = Post_H_Reflex, fill = Group)) +
geom_boxplot() +
theme_minimal() +
labs(title = "Post H-Reflex Amplitudes Across Treatment Groups",
y = "H-Reflex Amplitude (mV)")
print(plot)
# Perform paired t-tests within each group
results_t_tests <- data %>%
group_by(Group) %>%
summarise(
t_test_result = list(t.test(Post_H_Reflex, Pre_H_Reflex, paired = TRUE)),
p_value = t_test_result[[1]]$p.value
)
print(results_t_tests)
# Run one-way ANOVA to compare means between groups
anova_result <- aov(Post_H_Reflex ~ Group, data = data)
print(summary(anova_result))
# Normality check using Shapiro-Wilk test
normality_check <- by(data$Post_H_Reflex, data$Group, function(x) shapiro.test(x)$p.value)
print(normality_check)
# Homogeneity of variances using Levene's Test
levene_test_result <- leveneTest(Post_H_Reflex ~ Group, data = data)
print(levene_test_result)
# Tukey's HSD test for post-hoc comparisons
tukey_result <- TukeyHSD(anova_result)
print(tukey_result)
# Conduct power analysis
effect_size <- cohen.ES(test = "f", size = "medium")
power_analysis <- pwr.anova.test(k = length(groups), f = effect_size$effect.size, sig.level = 0.05, power = 0.8)
print(power_analysis)
# Write an explanation of results
cat("\nExplanation of Results:\n")
print(power_analysis)
# Write an explanation of results
cat("\nExplanation of Results:\n")
cat("The ANOVA results indicate whether there is a statistically significant difference in H-reflex amplitude across the four groups.\n")
cat("If the p-value from the ANOVA is less than 0.05, we reject the null hypothesis, which states there is no difference between groups.\n")
cat("The post-hoc Tukey test will specify which groups have significant differences in H-reflex amplitude.\n")
# Power analysis interpretation
cat("\nPower Analysis:\n")
cat("The power analysis was conducted to assess the probability of detecting an effect if there is one. The analysis showed that with the given sample size and a medium effect size, the power of the study is approximately ", power_analysis$power, ".\n")
# Conduct power analysis
effect_size <- cohen.ES(test = "f", size = "medium")
power_analysis1 <- pwr.anova.test(n = NULL, k = length(groups), f = effect_size$effect.size, sig.level = 0.05, power = 0.8)
print(power_analysis1)
power_analysis1 <- pwr.anova.test(n = 15, = length(groups), f = effect_size$effect.size, sig.level = 0.05, power = NULL)
print(power_analysis1)
power_analysis1 <- pwr.anova.test(n = 15, k = length(groups), f = effect_size$effect.size, sig.level = 0.05, power = NULL)
print(power_analysis1)
print(power_analysis1)
power_analysis1 <- pwr.anova.test(n = 60, k = length(groups), f = effect_size$effect.size, sig.level = 0.05, power = NULL)
print(power_analysis1)
# Conduct power analysis
power_analysis1 <- pwr.anova.test(n = NULL, k = 4, f = 0.15, sig.level = 0.05, power = 0.8)
print(power_analysis1) # n = 122.1209
power_analysis1 <- pwr.anova.test(n = 60, k = 4, f = 0.15, sig.level = 0.05, power = NULL)
print(power_analysis1) # n = .4652325
# Load necessary libraries
library(ggplot2)
library(dplyr)
library(pwr)
library(lme4)
library(car)
# Set seed for reproducibility
set.seed(42)
# Simulate sample data
n_per_group <- 15  # Number of participants per group
groups <- c("HROC_only", "VNS_only", "HROC_VNS", "SCI_only")
data <- data.frame(
ID = 1:(n_per_group * length(groups)),
Group = rep(groups, each = n_per_group),
Pre_H_Reflex = rnorm(n_per_group * length(groups), mean = 0.8, sd = 0.1),
Post_H_Reflex = c(
rnorm(n_per_group, mean = 0.85, sd = 0.12),
rnorm(n_per_group, mean = 0.88, sd = 0.12),
rnorm(n_per_group, mean = 1.0, sd = 0.12),
rnorm(n_per_group, mean = 0.78, sd = 0.12)
)
)
# View summary statistics
print(summary(data))
# Visualize pre- and post-intervention data by group
plot <- ggplot(data, aes(x = Group, y = Post_H_Reflex, fill = Group)) +
geom_boxplot() +
theme_minimal() +
labs(title = "Post H-Reflex Amplitudes Across Treatment Groups",
y = "H-Reflex Amplitude (mV)")
print(plot)
# Perform paired t-tests within each group
results_t_tests <- data %>%
group_by(Group) %>%
summarise(
t_test_result = list(t.test(Post_H_Reflex, Pre_H_Reflex, paired = TRUE)),
p_value = t_test_result[[1]]$p.value
)
print(results_t_tests)
print(plot)
# Perform paired t-tests within each group
results_t_tests <- data %>%
group_by(Group) %>%
summarise(
t_test_result = list(t.test(Post_H_Reflex, Pre_H_Reflex, paired = TRUE)),
p_value = t_test_result[[1]]$p.value
)
print(results_t_tests)
# Run one-way ANOVA to compare means between groups
anova_result <- aov(Post_H_Reflex ~ Group, data = data)
print(summary(anova_result))
# Normality check using Shapiro-Wilk test
normality_check <- by(data$Post_H_Reflex, data$Group, function(x) shapiro.test(x)$p.value)
print(normality_check)
# Homogeneity of variances using Levene's Test
levene_test_result <- leveneTest(Post_H_Reflex ~ Group, data = data)
print(levene_test_result)
# Tukey's HSD test for post-hoc comparisons
tukey_result <- TukeyHSD(anova_result)
print(tukey_result)
# Conduct power analysis
power_analysis1 <- pwr.anova.test(n = NULL, k = 4, f = 0.15, sig.level = 0.05, power = 0.8)
print(power_analysis1) # n = 122.1209
power_analysis1 <- pwr.anova.test(n = 60, k = 4, f = 0.15, sig.level = 0.05, power = NULL)
print(power_analysis1) # n = .4652325
# Write an explanation of results
cat("\nExplanation of Results:\n")
cat("The ANOVA results indicate whether there is a statistically significant difference in H-reflex amplitude across the four groups.\n")
cat("If the p-value from the ANOVA is less than 0.05, we reject the null hypothesis, which states there is no difference between groups.\n")
cat("The post-hoc Tukey test will specify which groups have significant differences in H-reflex amplitude.\n")
# Power analysis interpretation
cat("\nPower Analysis:\n")
cat("The power analysis was conducted to assess the probability of detecting an effect if there is one. The analysis showed that with the given sample size and a medium effect size, the power of the study is approximately ", power_analysis$power, ".\n")
cat("Since the power is greater than 0.8, the study has a high probability of detecting significant effects, if they exist.\n")
# Load necessary libraries
library(ggplot2)
library(dplyr)
library(pwr)
library(lme4)
library(car)
# Set seed for reproducibility
set.seed(42)
# Simulate sample data
n_per_group <- 15  # Number of participants per group
groups <- c("HROC_only", "VNS_only", "HROC_VNS", "SCI_only")
data <- data.frame(
ID = 1:(n_per_group * length(groups)),
Group = rep(groups, each = n_per_group),
Pre_H_Reflex = rnorm(n_per_group * length(groups), mean = 0.8, sd = 0.1),
Post_H_Reflex = c(
rnorm(n_per_group, mean = 0.85, sd = 0.12),
rnorm(n_per_group, mean = 0.88, sd = 0.12),
rnorm(n_per_group, mean = 1.0, sd = 0.12),
rnorm(n_per_group, mean = 0.78, sd = 0.12)
)
)
# View summary statistics
print(summary(data))
# Visualize pre- and post-intervention data by group
plot <- ggplot(data, aes(x = Group, y = Post_H_Reflex, fill = Group)) +
geom_boxplot() +
theme_minimal() +
labs(title = "Post H-Reflex Amplitudes Across Treatment Groups",
y = "H-Reflex Amplitude (mV)")
print(plot)
# Perform paired t-tests within each group
results_t_tests <- data %>%
group_by(Group) %>%
summarise(
t_test_result = list(t.test(Post_H_Reflex, Pre_H_Reflex, paired = TRUE)),
p_value = t_test_result[[1]]$p.value
)
print(results_t_tests)
# Run one-way ANOVA to compare means between groups
anova_result <- aov(Post_H_Reflex ~ Group, data = data)
print(summary(anova_result))
# QQ plot for residuals
qqnorm(residuals(anova_result))
qqline(residuals(anova_result), col = "red")
# Residuals vs fitted values plot
plot(anova_result, which = 1)
print(plot)
# Perform paired t-tests within each group
results_t_tests <- data %>%
group_by(Group) %>%
summarise(
t_test_result = list(t.test(Post_H_Reflex, Pre_H_Reflex, paired = TRUE)),
p_value = t_test_result[[1]]$p.value
)
print(results_t_tests)
# Run one-way ANOVA to compare means between groups
anova_result <- aov(Post_H_Reflex ~ Group, data = data)
print(summary(anova_result))
# QQ plot for residuals
qqnorm(residuals(anova_result))
qqline(residuals(anova_result), col = "red")
# Residuals vs fitted values plot
plot(anova_result, which = 1)
# Homogeneity of variances using Levene's Test
levene_test_result <- leveneTest(Post_H_Reflex ~ Group, data = data)
print(levene_test_result)
# Tukey's HSD test for post-hoc comparisons
tukey_result <- TukeyHSD(anova_result)
print(tukey_result)
# Conduct power analysis
effect_size <- cohen.ES(test = "f", size = "medium")
power_analysis <- pwr.anova.test(k = length(groups), f = effect_size$effect.size, sig.level = 0.05, power = 0.8)
print(power_analysis)
print(summary(anova_result))
print(summary(anova_result))
# Tukey's HSD test for post-hoc comparisons
tukey_result <- TukeyHSD(anova_result)
print(tukey_result)
# QQ plot for residuals
qqnorm(residuals(anova_result))
qqline(residuals(anova_result), col = "red")
# Residuals vs fitted values plot
plot(anova_result, which = 1)
# Homogeneity of variances using Levene's Test
levene_test_result <- leveneTest(Post_H_Reflex ~ Group, data = data)
print(levene_test_result)
# Shapiro-Wilk test for normality
shapiro_results <- by(data$Post_H_Reflex, data$Group, function(x) shapiro.test(x))
print(shapiro_results)
source("~/GitHub/yahriels/Stats/H-Reflex R Data/H-ReflexR.R")
source("C:/Users/dal866445/OneDrive - The University of Texas at Dallas/EMG Analysis and Stuff/H-Reflex R Script.R")
source("C:/Users/dal866445/OneDrive - The University of Texas at Dallas/EMG Analysis and Stuff/H-Reflex R Script.R")
