-------------------

@scripts/main.py  @src/hreflex_txbdc/model/stages/emg_characterization_stage.py @src/hreflex_txbdc/model/stages/mh_recruitment_curve_stage.py @src/hreflex_txbdc/model/stages/stage.py  @src/hreflex_txbdc/model/application_configuration.py  @src/hreflex_txbdc/model/background_worker.py @src/hreflex_txbdc/model/booth.py @src/hreflex_txbdc/model/emg_data_filter.py @src/hreflex_txbdc/model/fileio_helpers.py @src/hreflex_txbdc/model/open_ephys_streamer.py @src/hreflex_txbdc/model/session_message.py  @src/hreflex_txbdc/view/booth_selection_dialog.py @src/hreflex_txbdc/view/create_ad_hoc_booth_dialog.py @src/hreflex_txbdc/view/main_window.py @src/hreflex_txbdc/view/no_booths_found_dialog.py  

---------
Can you actually go ahead help me significantly simplify this entire notebook @  by taking each section and making/breaking it out into a function, I want it to be as simplified as possible by turning them all into functions. If you need a place to put all of these functions them you can use @src/open_ephys/analysis/helpers.py
---------


I am bringing in and updating some of the channel assignments.
These will be new assignments, I want to update my app to bring in these channels and update my data files to also track these signals (if needed). Also in my previous version "Stim ADC" was the tibial stim waveform, I want to update the assignment so my app now tracks "Tibial Stim" ADC channel instead. The other channels will be passed through the app and recorded passively for now until I assignm them functions. 


Channel index	Signal	Mode
ch0	Unipolar filtered EMG	ONLINE / raw EMG electrode 1 in OFFLINE
ch1	Bipolar filtered+abs EMG	ONLINE / raw EMG electrode 2 in OFFLINE
ch2	Tibial ADC sync line (TTL)	both modes (ADC1)
ch3	Tibial Stim ADC waveform	both modes (ADC2)
ch4	VNS ADC sync line (TTL)	both modes (ADC3)
ch5	VNS ADC waveform	both modes (ADC4)
ch6	Feeder ADC trigger (Feeder)	(ADC5)

These are my Digital Output Assignments
Channel index	Signal	Mode
DIGOUT1	Tibial Stim	
DIGOUT2	VNS Stim
DIGOUT3	Feeder	
-----------
Updating calculations

M-H Measurements
- M-H Peak = Mean Rectified Average Amplitude Value (Absolute Value) of All Sampled Data within M-H Window
- H/M Ratio = ((H-Peak)/(M-Peak))
- M-H Size = M-H size was calculated as average EMG amplitude in the M-H window interval minus pre-stim activity average background EMG amplitude and expressed in units of average background EMG amplitude.


----------
I need to updated my analysis to accommodate the new changes I made to my app 

rom the code I've already read, version 7 adds two fields to MhRecruitmentCurveTrial for the digital onset detection introduced this session. Here's the exact diff from mh_recruitment_curve_data_file.py:

New instance variables (initialized in __init__):


self.digital_onset_sample_num: int = -1  # int64 — absolute OE sample counter of the DIGITAL IN rising edge
self.digital_onset_channel: int = -1     # int32 — OE digital channel index (0-indexed)
read_from_file — version 7 block (reads after the existing v5/v6 blocks):


if (file_version >= 7):
    self.digital_onset_sample_num = FileIO_Helpers.read(fid, "int64")
    self.digital_onset_channel    = FileIO_Helpers.read(fid, "int32")
save_to_file — appended at end (always written, so v7 files always include them):


FileIO_Helpers.write(fid, "int64", self.digital_onset_sample_num)
FileIO_Helpers.write(fid, "int32", self.digital_onset_channel)
File header — stage now writes version 7 instead of 6:


MhRecruitmentCurveHeader(7, ...)
Summary of what these fields capture:

Field	Type	Meaning
digital_onset_sample_num	int64	Absolute Open Ephys sample counter at the rising edge of the DIGITAL IN TTL — exact stimulus onset with sub-sample precision. -1 if no digital event was detected (ADC fallback or no onset).
digital_onset_channel	int32	The OE digital channel index (0-indexed) that fired. -1 if unused.
Older files (v0–v6) read back cleanly — both fields default to -1, which matches onset_detected != 2 in offline analysis. Only v7+ files will have the actual digital event data.



So I want my codes across my database to switch to using the Digital line for onset detection instead of the Analog in/ADC line. 