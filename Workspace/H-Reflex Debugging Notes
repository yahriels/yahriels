WOrk and Breakpoints on:
Mainwindow.py -> def On_data_recieved
background worker.py


line 127 of MainWindow.py:
        # Initialize the threadpool and the background worker
        self.threadpool = QThreadPool()
        self.background_worker = BackgroundWorker()
        self.background_worker.signals.data_received_signal.connect(self._on_data_received)
        self.threadpool.start(self.background_worker)

UNderstand channel logic on lines 58-82 of backgroundworker.py


Trial logic of when trial is initiated, how to initiate trial, when to stimulate ... 
-> Look at the stage files 

Where is the bug
-> In pulling data?
-> In processing data?
-> In Plotting trials

-------------------

@scripts/main.py  @src/hreflex_txbdc/model/stages/emg_characterization_stage.py @src/hreflex_txbdc/model/stages/mh_recruitment_curve_stage.py @src/hreflex_txbdc/model/stages/stage.py  @src/hreflex_txbdc/model/application_configuration.py  @src/hreflex_txbdc/model/background_worker.py @src/hreflex_txbdc/model/booth.py @src/hreflex_txbdc/model/emg_data_filter.py @src/hreflex_txbdc/model/fileio_helpers.py @src/hreflex_txbdc/model/open_ephys_streamer.py @src/hreflex_txbdc/model/session_message.py  @src/hreflex_txbdc/view/booth_selection_dialog.py @src/hreflex_txbdc/view/create_ad_hoc_booth_dialog.py @src/hreflex_txbdc/view/main_window.py @src/hreflex_txbdc/view/no_booths_found_dialog.py  

---------
On this app, on the recruitment curve stage, the app currently has an option to "Choose trial signals" on the "most recent trial" plot. Currently there is the differentially subtracted (bipolar) and filtered signal (default), and then the differentially subtracted (bipolar) then filtered and absolute value signal (gray), and then the ADC line (green). I want to add to this field/plot the option to view the non-differentially subtracted (unipolar)  and filtered data in orange, and the non-differentially subtracted (unpolar) then filtered and absolute value data of in a grayish purple color.  On the recruitment curve plot, I also want to add the option to toggle to view the unipolar recruitment curve where I want to add traces for the unipolar data where it goes through the exact same method of acquiring and building the data but just using the unipolar data instead, You can keep the color similar but make it lighter and differentiate it with dashed lines instead of solid line. So for the traces of the m-wave peaks in this unipolar recruitment curve it can be a lighter green and will use dashed lines instead of solid lines, for the h-wave peaks traces you can use a lighter blue with dashes lines instead of solid lines.  
